// Array of favorite books
const favoriteBooks = ['Maths','Physics','Chemistry'];
    function displayBooks(books) {
    for (let i = 0; i < books.length; i++) {
        console.log(books[i]);
    }
}
displayBooks(favoriteBooks);